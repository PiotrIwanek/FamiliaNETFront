export class CategoryConverter {

  constructor() {
  }

  // public static createCategory (category: any) {
  //     return new Category( category.id , category.name , this.createCategory(category.parent),
  //                         category.children.map(data => {return this.createCategory(data)} ))
  // }
}
